// Sample unit test so the ShipItAnyway workflow's "Run unit tests" step does
// real, passing work. Replace with your project's actual test suite.
const { test } = require('node:test');
const assert = require('node:assert');

test('ci sanity: math holds', () => {
  assert.strictEqual(1 + 1, 2);
});
